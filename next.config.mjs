/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ["raw.githubusercontent.com", "images.unsplash.com", "assets.aceternity.com", "aceternity.com"],
    },
  };
  
  export default nextConfig;